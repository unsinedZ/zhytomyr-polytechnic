name: push_notification
description: Notifications via firebase messaging
version: 0.0.1
author: Softwine
publish_to: none

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^9.1.1
  shared_preferences: ^2.0.5
  rxdart: ^0.26.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
